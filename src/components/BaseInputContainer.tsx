import styled from "styled-components";

const BaseInputContainer = styled.article`
  display: flex;
  width: 100%;
  align-items: center;
  margin: 0.5rem auto;
`;

export default BaseInputContainer;
